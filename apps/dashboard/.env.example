# Base URL of the API (apps/api)
VITE_API_URL=http://localhost:3001

# Must match API_SECRET in apps/api/.env — sent as a Bearer token on /clients calls
VITE_API_SECRET=your-random-secret-for-widget-auth
